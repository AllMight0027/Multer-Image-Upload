<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="demo.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Multer Image Uplaoder</title>
</head>
<body>
    <div class="container py-5">

        <!-- For demo purpose -->
        <header class="text-white text-center">
            <h1 class="display-4">Multer Image Uploader</h1>
            <img src="https://res.cloudinary.com/mhmd/image/upload/v1564991372/image_pxlho1.svg" alt="" width="150" class="mb-4">
        </header>
    
    
        <div class="row py-4">
            <div class="col-lg-6 mx-auto">
    
                <!-- Upload image input-->
                <form class="input-group mb-3 px-2 py-2 rounded-pill bg-white shadow-sm" action='/upload' method="POST" enctype="multipart/form-data">
                    <input id="upload" type="file"  name="profilepic">
                    <label id="upload-label" for="upload" class="font-weight-light text-muted">Choose file</label>
                    <div class="input-group-append">
                        <label for="upload" class="btn btn-light m-0 rounded-pill px-4"> <i class="fa fa-cloud-upload mr-2 text-muted"></i><small class="text-uppercase font-weight-bold text-muted">Browse</small></label> <br>
                    </div>
                    <br>
                    <input type="submit" value="Sumbit" class="text-uppercase font-weight-bold text-muted">
                </form>
    
                <!-- Uploaded image area-->
                <p class="font-italic text-center" style="color: brown; opacity: 0;"><%= typeof message == 'undefined'? '': message %></p>
                <div class="image-area mt-4"><img id="imageResult" src="<%= typeof filename == 'undefined' ? '' : filename %> "  class="img-fluid rounded shadow-sm mx-auto d-block"></div>
    
            </div>
        </div>
    </div>
</body>
</html>